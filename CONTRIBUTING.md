# Contributing

- preserve the current Telegram command set unless you are changing behavior on purpose
- run `python -m py_compile *.py` before sending changes
- keep `README.md`, `config.example.py` and `.env.example` aligned
- do not commit `.env`, `config.py`, the SQLite database or virtual environments
